---
description: Create a verification loop for FEATURE_NAME (installed APK, not a Compose host)
---

Follow `.claude/skills/create-verifier/SKILL.md` in full. FEATURE_NAME is the argument after this command (example: `/create-verifier capture-mode`).
